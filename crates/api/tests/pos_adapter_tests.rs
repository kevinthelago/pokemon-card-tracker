// Integration test entry point for POS adapters.
// Contract tests live in the source modules under #[cfg(test)] since
// the api crate is a binary (not a lib).
//
// Run with: cargo test -p api -- pos
